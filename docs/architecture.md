# 架构草图

## 1. 模块划分

- `Simulator`：统一调度执行、重置、装载程序
- `Memory`：字节寻址内存与边界检查
- `load_elf()`：负责程序映射与入口地址解析
- `Debugger`：命令行调试控制

## 2. 先后实现顺序

1. ELF 解析
2. RV32I 指令解码
3. M 扩展
4. syscall
5. 调试器断点与单步
6. 可选流水线
